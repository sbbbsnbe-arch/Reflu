import React from 'react'
import ReactDOM from 'react-dom/client'
import RufloApp from '../ruflo-app'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RufloApp />
  </React.StrictMode>
)
